<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Bridge traffic is plaintext ws:// on LAN/Tailscale; required for release builds. -->
    <uses-permission android:name="android.permission.INTERNET"/>
    <!-- mDNS 局域网发现(bonsoir):Android 收发组播包必须显式申请 -->
    <uses-permission android:name="android.permission.CHANGE_WIFI_MULTICAST_STATE"/>
    <!-- WebRTC(flutter_webrtc)的网络监听器要调 ConnectivityManager,没这个权限
         libjingle 的 network_thread 会 JNI 异常后 CHECK 失败直接 SIGABRT(闪退)。
         flutter_webrtc 自身 manifest 是空的,不会传递带入,必须我们声明。 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <!-- bindProcessToNetwork 用于在 LAN 直连建链时选择 Wi-Fi 网络 -->
    <uses-permission android:name="android.permission.CHANGE_NETWORK_STATE"/>
    <!-- Android 13+ 通知运行时权限(任务完成/扩展等待输入提醒) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- 前台服务保活 WebSocket:后台时也能实时收到 bridge 推送的完成事件 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>
    <application
        android:label="PiPilot"
        android:name="${applicationName}"
        android:usesCleartextTraffic="true"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">
        <!-- enableOnBackInvokedCallback:开启预测性返回手势(侧滑按住时
             预览回到桌面/上一页)。Android 15+ 默认强制开启,13/14 必须
             显式声明才有预览动画。PopScope 拦截时(抽屉/灵动岛/模型选择
             展开)引擎会注册 back callback,此时不显示预览、松手走我们的
             分发逻辑,行为不变。 -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- 自有前台服务同时持有 partial wake lock,确保后台能处理 WebSocket 心跳。
             任务从最近任务列表移除时停止,避免留下无 Flutter engine 的空服务。 -->
        <service
            android:name=".KeepAliveService"
            android:exported="false"
            android:stopWithTask="true"
            android:foregroundServiceType="dataSync"/>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
